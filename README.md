# AI C++ IDE

> 基于 PyQt6 和 Moonshot AI (Kimi) 构建的智能 C++ 代码分析、修复与可视化 IDE。
> 
> **当前版本：v0.4.0**

---

## 📖 项目概述

本项目是一个 **AI 驱动的 C++ 集成开发环境（IDE）**，通过调用大语言模型（LLM）为 C++ 开发者提供实时的代码分析、Bug 检测、自动修复等功能。项目采用 **模块化架构** 设计，支持多轮对话上下文、JSON 结构化输出、智能 Agent 自主决策，以及**可视化错误标注**等高级特性。

### 核心设计理念
- **AI 赋能**：利用大模型深度理解 C++ 代码语义，发现传统静态分析工具难以察觉的逻辑缺陷
- **Agent 智能**：系统可自主判断代码问题的严重程度，自动决策是仅输出分析报告，还是直接提供修复代码
- **可视化交互**：分析结果与代码编辑器深度联动，支持行号红点标注、点击跳转、Hover 提示等 IDE 级体验
- **工程化封装**：SDK 层封装了重试机制、JSON 清洗、上下文管理等复杂逻辑，上层业务只需关注 Prompt 设计

---

## ✨ 主要功能

| 功能模块 | 描述 | 交互特性 |
|---------|------|---------|
| 🔍 **代码分析** | 8维度全面分析：Bugs/线程/内存/安全/可读性/可维护性/性能/建议 | **HTML 渲染结果**，行号可点击跳转，问题行自动标红 |
| 🛠 **代码修复** | 自动修复代码中的潜在问题，保持原有逻辑不变 | 完整修复后的代码，支持语法高亮 |
| 🧠 **智能 Agent** | 自主判断问题严重程度：严重则修复，轻微则仅分析 | 修复代码 / JSON 分析 |
| 📝 **代码编辑器** | 支持行号显示、C++ 语法高亮、深色主题 | 行号红点标记问题行，Hover 显示错误描述 |
| 📂 **文件操作** | 打开 `.cpp` / `.h` 文件、保存分析结果 | - |
| ⏳ **进度指示** | 操作进行时显示进度状态，按钮禁用防重复提交 | 实时状态反馈 |

### 🔥 增强版新特性

- **行号红点标注**：分析结果中的问题行，在编辑器行号区显示红色圆点标记
- **点击跳转**：分析结果中的行号链接可点击，自动跳转到代码对应位置
- **Hover 提示**：鼠标悬停在问题行时，显示详细的错误描述 Tooltip
- **行高亮**：问题行自动高亮为红色背景，跳转时当前行高亮为黄色背景
- **全语法高亮**：所有代码展示区域（输入、修复、Agent输出）均支持 C++ 语法高亮
- **进度指示**：AI 操作期间显示进度状态，禁用按钮防止重复提交

---

## 🏗 项目架构

```
AI_IDE_demo/
├── main.py                    # 程序入口
├── requirements.txt           # 依赖清单
├── README.md                  # 项目说明文档
│
├── core/                      # 🔧 核心 SDK 层
│   └── API_Call_SDK_v1.py     # AI 客户端封装（重试、上下文、JSON清洗）
│
├── analyzer/                  # 🧠 分析器层
│   ├── cpp_analyzer.py        # C++ 代码分析器
│   ├── code_fixer.py          # 代码修复器
│   └── code_agent.py          # 智能 Agent（自主决策）
│
├── ui/                        # 🖥 UI 层
│   ├── main_window.py         # 主窗口（Tab布局、Worker线程、跳转逻辑）
│   ├── code_editor.py         # 自定义代码编辑器（行号、红点、Hover提示）
│   └── cpp_highlighter.py     # C++ 语法高亮器
│
├── embody_intelligence/       # 🤖 机器人坐标变换演示模块
│   └── test1.py               # NumPy 齐次变换矩阵计算示例
│
├── .vscode/                   # VSCode 配置
├── .idea/                     # PyCharm 配置
│
└── test.cpp                   # 测试用 C++ 代码
```

### 架构亮点

#### 1. SDK 层（`core/API_Call_SDK_v1.py`）
- **多轮对话上下文管理**：自动维护对话历史，支持上下文裁剪（保留最近 20 轮）
- **JSON 模式隔离**：结构化输出使用独立上下文，防止污染对话历史
- **智能重试机制**：遇到模型繁忙（429/overloaded）自动重试，最多 3 次
- **JSON 清洗与容错**：自动去除 Markdown 代码块标记，解析失败时尝试让 AI 自我修复

#### 2. 分析器层（`analyzer/`）
- **职责分离**：分析、修复、Agent 三个模块独立，便于扩展和维护
- **Prompt 工程**：每个模块有精心设计的 Prompt，指导 AI 按预期格式输出
- **CodeAgent 自主决策**：根据问题严重程度动态选择输出修复代码或 JSON 分析报告

#### 3. UI 层（`ui/`）
- **Worker 线程**：所有 AI 操作均在独立线程执行，避免阻塞 UI
- **Tab 布局**：分析结果、修复代码、Agent 输出三栏分离，清晰直观
- **可视化错误标注**：分析结果与编辑器联动，支持红点、高亮、跳转、Hover 提示
- **自定义编辑器**：基于 `QPlainTextEdit` 实现，支持行号和 C++ 语法高亮

---

## 🚀 快速开始

### 环境要求
- Python 3.10+
- Moonshot AI API Key（[获取地址](https://platform.moonshot.cn/)）

### 安装依赖

```bash
pip install -r requirements.txt
```

### 配置 API Key

```bash
# Windows PowerShell
$env:MOONSHOT_API_KEY = "your-api-key-here"

# Windows CMD
set MOONSHOT_API_KEY=your-api-key-here

# Linux / macOS
export MOONSHOT_API_KEY="your-api-key-here"
```

### 运行程序

```bash
python main.py
```

---

## 📸 使用指南

1. **打开代码文件**：点击菜单栏 `文件 → 打开.cpp文件`，或直接粘贴代码到左侧编辑器
2. **代码分析**：点击 `分析` 按钮，AI 将检测 Bugs、线程问题、内存问题等
   - 结果以 **HTML 格式**显示在"分析结果"标签页
   - 点击结果中的行号链接，自动跳转到代码对应位置
   - 问题行在编辑器中**自动标红**，行号区显示**红点**
   - 鼠标悬停在问题行，显示**错误描述 Tooltip**
3. **代码修复**：点击 `修复` 按钮，AI 将返回完整修复后的代码，显示在"修复代码"标签页
4. **智能 Agent**：点击 `🧠 智能` 按钮，Agent 将自主判断代码问题的严重程度：
   - 若存在严重问题（内存泄漏、崩溃风险、未定义行为）→ 输出修复后的完整代码
   - 若问题轻微 → 输出 JSON 格式的分析报告
5. **保存结果**：点击 `文件 → 保存结果`，将分析报告导出为文本文件

---

## 🛠 技术栈

| 技术 | 用途 |
|-----|------|
| **PyQt6** | 跨平台 GUI 框架，构建 IDE 界面 |
| **OpenAI SDK** | 调用 Moonshot AI API（兼容 OpenAI 接口） |
| **Kimi-k2.5** | 大语言模型，提供代码分析与修复能力 |
| **NumPy** | 机器人坐标变换演示模块的计算库 |

---

## 📂 文件说明

| 文件 | 说明 |
|-----|------|
| `main.py` | 程序入口，初始化 QApplication 和主窗口 |
| `core/API_Call_SDK_v1.py` | AI 客户端 SDK，封装了完整的 LLM 调用逻辑（重试、上下文、JSON清洗） |
| `analyzer/cpp_analyzer.py` | C++ 代码分析器，输出 JSON 格式的分析报告（含行号信息） |
| `analyzer/code_fixer.py` | 代码修复器，返回修复后的完整代码 |
| `analyzer/code_agent.py` | 智能 Agent，根据问题严重程度自主决策 |
| `ui/main_window.py` | 主窗口，包含菜单栏、编辑器、Tab结果区、按钮区、跳转逻辑 |
| `ui/code_editor.py` | 自定义代码编辑器组件，支持行号显示、红点标注、Hover提示 |
| `ui/cpp_highlighter.py` | C++ 语法高亮器，基于 QSyntaxHighlighter |
| `ui/diff_view.py` | 代码差异对比视图，左右分栏显示删除（红）/新增（绿）差异 |
| `ui/settings_dialog.py` | 设置对话框，支持 API 配置、模型选择、主题切换 |
| `utils/config.py` | 配置管理模块，支持加载/保存/默认配置回退 |
| `embody_intelligence/test1.py` | 机器人坐标变换演示：齐次变换矩阵计算示例 |
| `test.cpp` | 测试用 C++ 代码文件 |

---

## 🔮 未来展望

- [ ] 支持更多编程语言（Python、JavaScript 等）
- [ ] 集成静态分析工具（如 Clang-Tidy、Cppcheck）作为 AI 的辅助输入
- [ ] 支持代码 diff 可视化对比
- [ ] 添加项目文件树，支持多文件管理
- [ ] 引入本地模型支持，降低 API 调用成本
- [ ] 支持实时代码补全（Inline Completion）

---

## 📄 License

MIT License

---

## 📋 版本历史

### v0.4.0 (2026-04-08)
- ✨ **设置面板**：新增 `SettingsDialog` 设置对话框，支持配置 API Key、Base URL、模型、温度参数
- ✨ **配置持久化**：设置保存到 `config/settings.json`，支持环境变量和配置文件双重读取
- ✨ **模型切换**：支持在设置中切换 Kimi 模型（k2.5/k1.5/k1）
- ✨ **温度调节**：支持调整 Temperature 参数（0.0-2.0）
- ✨ **配置热重载**：菜单支持"重载配置"功能，无需重启程序
- 🔧 **SDK增强**：`AIClient` 支持从配置文件读取参数，优先环境变量 fallback 到配置文件
- 📁 **新增模块**：`utils/config.py` 配置管理模块，`ui/settings_dialog.py` 设置对话框

### v0.3.0 (2026-04-08)
- ✨ **代码对比视图（Diff）**：新增 `DiffView` 组件，修复后自动显示原始代码与修复代码的差异对比
- ✨ **差异可视化**：删除行标红、新增行标绿，左右分栏同步滚动
- ✨ **Tab标签页优化**：分析/修复/对比/Agent四个标签页，使用emoji图标
- ✨ **Diff语法高亮**：对比视图两侧均支持C++语法高亮

### v0.2.0 (2026-04-08)
- ✨ **增强代码分析器**：分析维度从4个扩展到8个（新增安全/可读性/可维护性/性能）
- ✨ **添加进度指示器**：AI操作期间显示进度状态，按钮自动禁用防重复提交
- ✨ **增强语法高亮**：C++关键字从20个扩充到49个
- ✨ **分析结果增强**：显示代码总结、复杂度评估、严重程度emoji标识
- 🐛 **修复Agent路径问题**：`code_agent.py` 添加 `sys.path` 设置
- ♻️ **重构UI代码**：修复和Agent操作完成后正确恢复按钮状态

### v0.1.0 (2026-04-08)
- 🎉 项目初始化
- ✅ 基础AI C++ IDE功能（分析、修复、Agent）
- ✅ 代码编辑器（行号、语法高亮）
- ✅ 可视化错误标注（红点、跳转、Hover提示）

---

> 本项目为 AI IDE 技术演示项目，展示了如何将大语言模型能力封装为具有可视化交互的实用开发工具。
